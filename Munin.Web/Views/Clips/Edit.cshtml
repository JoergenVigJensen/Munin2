@model Munin.DAL.Models.Clip

@{
    ViewBag.Title = "Edit";
}

<div ng-controller="clipsFormCtrl as ctrl" ng-init="initpage()">
    @Html.Hidden("ID", Model.ClipId, new { @id = "ID" })
    @Html.Partial("_Form")
</div>


<div>
    @Html.ActionLink("Tilbage til liste", "Index")
</div>